# Echo-Orchestra Python dependencies
# sqlite3 is Python stdlib — do NOT add it here.

# Core
requests>=2.32.0
pydantic>=2.7.0
python-dotenv>=1.0.1
tenacity>=8.3.0

# Scheduling (persistent — survives restarts)
apscheduler>=3.10.4
sqlalchemy>=2.0.30

# Device detection
psutil>=5.9.8

# HTTP API (Stripe webhook + optional admin endpoints)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
stripe>=9.0.0

# OpenAI-compatible client (used for Perplexity and LiteLLM proxy calls)
openai>=1.35.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
